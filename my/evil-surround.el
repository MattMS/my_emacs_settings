; ## Main links
;
; - [GitHub](https://github.com/emacs-evil/evil-surround)
;
; ## Key-bindings
;
; - `vi)` select everything between the brackets.
; - `yi)` copy everything between the brackets.
;
; ---

; Do not need to require when using `package-initialize`.
;(require 'evil-surround)

(global-evil-surround-mode 1)
